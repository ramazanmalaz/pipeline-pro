pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Welcome to Jenkins World Rama'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
